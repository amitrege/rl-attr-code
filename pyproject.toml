[project]
name = "rl-attr"
version = "0.1.0"
description = "Work-in-progress code for RL attribution experiments."
requires-python = ">=3.11"

[project.optional-dependencies]
approx = [
    "gymnasium>=1.0",
    "matplotlib>=3.8",
    "numpy>=1.26,<2",
    "pandas>=2.2",
    "torch>=2.4",
]
